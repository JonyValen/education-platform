<!DOCTYPE html>
<html>
    <head>
        <title>Gyde Research & Education</title>
        <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="/stylesheets/root/root.css" type="text/css">
    </head>
    <body>
        <nav class="navbar navbar-default" id="topNav">
            <div class="container-fluid">
               <div class="navbar-header">
                  <a id="gyde" class="navbar-brand" href="/">Gyde</a>
               </div>
            <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav navbar-right">
               <% if(!currentUser) { %>
                   <form class="form-inline" action="/login" method="POST">
                      <div class="form-group" id="login1">
                         <label for="email" id="label1">Email</label><br />
                         <input type="text" id="email" name="email">
                      </div>
                      <div class="form-group" id="login2">
                          <label for="password" id="label2">Password</label><br />
                          <div>
                             <input type="password" id="password" name="password">
                             <button class="btn btn-primary btn-xs">Log In</button>
                          </div>
                      
                      </div>
                   
                    </form>
               <% } else{ %>
                   <li><a href="#">Signed in as: <%= currentUser.first + ' ' + currentUser.last %></a></li>
                   <li><a href="/logout">Logout</a></li>
               <% } %>
               
                
            </ul>
            </div>
            </div>
           
         </nav>
  